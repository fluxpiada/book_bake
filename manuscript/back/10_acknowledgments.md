# Acknowledgments

Back matter lives in `manuscript/back/`. It always goes into the EPUB. It only
goes into the PDF if you pass `--with-extras`, and then it lands after the end
marker, where it belongs.
